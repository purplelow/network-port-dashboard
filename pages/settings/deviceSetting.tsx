import type { NextPage } from "next";
import Layout from "../../components/layout/layout";

const Home: NextPage = () => {
  return (
    <Layout title="장비 기본설정">
      <div>장비 기본설정</div>
    </Layout>
  );
};

export default Home;
