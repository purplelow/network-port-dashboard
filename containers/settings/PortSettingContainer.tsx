import type { NextPage } from "next";
import Layout from "../../components/layout";

const Home: NextPage = () => {
  return (
    <Layout title="포트 설정">
      <div>포트 설정</div>
    </Layout>
  );
};

export default Home;
